info:
  name: validation test
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost/validation
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
