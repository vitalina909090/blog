info:
  name: order.process
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:80/orders
  body:
    type: json
    data: |-
      {
        "amount": 100
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
