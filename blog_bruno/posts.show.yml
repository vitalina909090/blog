info:
  name: posts.show
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:80/posts/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
